@using QRTech.Models
@model Line
@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@if (ViewBag.islem == 1)
{
    <div class="alert alert-success" role="alert">
        Hat Ekleme İşlemi Başarıyla Gerçekleşti!
    </div>
}
else if (ViewBag.islem == -1)
{
    <div class="alert alert-danger" role="alert">
        Hat Ekleme İşlemi Başarısız!
    </div>
}
else
{

}

<div class="wrapper">

    <div id="content">
        <h2>  Hat Ekle</h2>
        <br />
        <div class="container-fluid">
            @using (Html.BeginForm("LineCreate", "Line", FormMethod.Post))
            {
                <form action="" class="was-validated">

                    <div class="form-group">
                        <label for="uname">Hat Adı:</label>
                        @Html.TextBox("HatNo", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="uname">Başlangıç Durağı:</label>
                        @Html.TextBox("BaslangicDurak", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="e-posta">Bitiş Durağı:</label>
                        @Html.TextBox("BitisDurak", null, new { @class = "form-control" })
                    </div>
                    <div class="form-group">
                        <label for="e-posta">Normal Fiyat:</label>
                        @Html.TextBox("tamFiyat", null, new { @class = "form-control" })
                    </div>
                    <div class="form-group">
                        <label for="e-posta">Öğrenci Fiyat:</label>
                        @Html.TextBox("ogrenciFiyat", null, new { @class = "form-control" })
                    </div>
                    <br />
                    <button type="submit" class="btn btn-success">Kaydet</button>
                </form>
            }
        </div>
    </div>
</div>