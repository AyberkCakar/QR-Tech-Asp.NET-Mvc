@using QRTech.Models
@model Driver
@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@if (ViewBag.islem == 1)
{
    <div class="alert alert-success" role="alert">
        Şoför Ekleme İşlemi Başarıyla Gerçekleşti!
    </div>
}
else if (ViewBag.islem == -1)
{
    <div class="alert alert-danger" role="alert">
        Şoför Ekleme İşlemi Başarısız!
    </div>
}
else
{

}
<div class="wrapper">

    <div id="content">
        <h2>  Şöför Ekle</h2>
        <br />
        <div class="container-fluid">
            @using (Html.BeginForm("DriverCreate", "Driver", FormMethod.Post))
            {
                <form action="" class="was-validated">

                    <div class="form-group">
                        <label for="uname">Söför Adı:</label>
                        @Html.TextBox("AD", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="uname">Soyadı:</label>
                        @Html.TextBox("Soyad", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="e-posta">TC:</label>
                        @Html.TextBox("TC", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="e-posta">Ehliyet Seri No:</label>
                        @Html.TextBox("ehliyetSeriNo", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="drop_list">Maaş:</label>
                        @Html.TextBox("Maas", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="drop_list">Kullandığı Araç Plaka:</label>
                        @Html.TextBox("AracPlaka", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="drop_list">Telefon:</label>
                        @Html.TextBox("Telefon", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="drop_list">Mail:</label>
                        @Html.TextBox("Mail", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="drop_list">Adres:</label>
                        @Html.TextBox("Adres", null, new { @class = "form-control" })
                    </div>
                    <br />
                    <button type="submit" class="btn btn-success">Ekle</button>
                </form>
            }
        </div>
    </div>
</div>
