@using QRTech.Models
@model Vehicle
@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@if (ViewBag.islem == 1)
{
    <div class="alert alert-success" role="alert">
        Araç Ekleme İşlemi Başarıyla Gerçekleşti!
    </div>
}
else if (ViewBag.islem == -1)
{
    <div class="alert alert-danger" role="alert">
        Araç Ekleme İşlemi Başarısız!
    </div>
}
else
{

}

<div class="wrapper">

    <div id="content">
        <h2>  Araç Ekle</h2>
        <br />
        <div class="container-fluid">
            @using (Html.BeginForm("VehicleCreate", "Vehicle", FormMethod.Post))
            {
                <form action="" class="was-validated">

                    <div class="form-group">
                        <label for="e-posta">Araç Plaka:</label>
                        @Html.TextBox("Plaka", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="e-posta">Araç Marka:</label>
                        @Html.TextBox("AracMarka", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="e-posta">Araç Model:</label>
                        @Html.TextBox("Model", null, new { @class = "form-control" })

                    </div>

                    <div class="form-group">
                        <label for="e-posta">Renk:</label>
                        @Html.TextBox("Renk", null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        <label for="e-posta">Araç Şase Numarası:</label>
                        @Html.TextBox("saseNumarasi", null, new { @class = "form-control" })
                    </div>
                    <div class="form-group">
                        <label for="drop_list">Engelli Desteği:</label>
                        @Html.TextBox("engelliDestegi", null, new { @class = "form-control" })

                    </div>
                    <div class="form-group">
                        <label for="drop_list">Kullanıldığı Hat:</label>
                        @Html.TextBox("HatNo", null, new { @class = "form-control" })

                    </div>
                    <br />
                    <button type="submit" class="btn btn-success">Aracı Ekle</button>
                </form>
            }

        </div>
    </div>
</div>
